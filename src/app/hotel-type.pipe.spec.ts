import { HotelTypePipe } from './hotel-type.pipe';

describe('HotelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new HotelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
